var backgroundChange = document.getElementsByClassName("background-color-change");
var divChangeClass = document.getElementsByClassName("calculator")

backgroundChange.addEventListener("click", backgroundColorChange);

function backgroundColorChange()
{
    div.className.replace("calculator", "calculator-alt");
}
